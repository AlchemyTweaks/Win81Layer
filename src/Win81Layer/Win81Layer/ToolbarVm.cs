namespace Win81Layer;

// One classic taskbar "Toolbar" (a folder shown as a shortcuts button next to the tray).
public sealed class ToolbarVm
{
	public string Name { get; set; } = "";

	public string Path { get; set; } = "";
}
